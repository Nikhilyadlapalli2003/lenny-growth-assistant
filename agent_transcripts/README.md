# Agent Transcripts

This folder holds the coding-agent session logs used to build this project, per the assignment's
requirement to include transcripts (successes and corrected mistakes), with secrets removed.

- `01_initial_scaffolding.md` — backend/frontend project structure, config layer, DB models.
- `02_debugging_pgvector_indexing.md` — an example of a failed approach and the fix.

When you export your own real agent session logs (Claude Code, Cursor, etc.), replace these
placeholders and make sure no API keys or credentials are present before committing.
